<h1>Your shopping Cart</h1>

<ul>
  <% @cart_items.each do |item| %>
    <li> <%= "#{item.product.name} for #{item.product.price}€, #{item.product.product_category.name} "  %>
         <%= link_to 'Delete', cart_item_path(:cart_item_id => item.id), method: :delete %>
    </li>
  <% end  %>
</ul>
